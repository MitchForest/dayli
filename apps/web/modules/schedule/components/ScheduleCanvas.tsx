'use client';

import { ScheduleView } from './ScheduleView';

// This component is now just a wrapper since ScheduleView handles everything
export function ScheduleCanvas() {
  return <ScheduleView />;
} 